# Ignore everything in the project root
/*

# Except the src directory
!/src/

# Except this .gitignore file itself
!/.gitignore

# Ignore compiled Java class files inside the src directory (or anywhere else)
*.class

# Ignore the downloaded JDK zip if it's still there
jdk.zip
