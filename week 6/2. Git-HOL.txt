# Create a .log file and a log folder in the working directory
touch error.log
mkdir log
touch log/app.log

# Update the .gitignore file to ignore .log extensions and log folders
echo "*.log" > .gitignore
echo "log/" >> .gitignore

# Verify if the git status reflects the changes
git status
